# 开发环境配置
# 使用 docker-compose.dev.yml 时，将本文件复制为 `.env`：
# cp .env.dev.example .env

# docker-compose.dev.yml 会在同一个 Compose 网络中启动 PostgreSQL。
# 应用容器会把 POSTGRES_HOST 覆盖为 `postgres`。
POSTGRES_HOST="localhost"
POSTGRES_PORT="5432"
POSTGRES_USER="user_service"
POSTGRES_PASSWORD="change-me"
POSTGRES_DB="user_service"
POSTGRES_SCHEMA="public"
# 可选。PostgreSQL 会话时区，docker-compose.dev.yml 默认值为 Asia/Shanghai。
POSTGRES_TIMEZONE="Asia/Shanghai"
# 可选。PostgreSQL 连接要求 SSL 时设置为 require。
POSTGRES_SSLMODE=""
# 可选。额外的 PostgreSQL URL 查询参数，例如 connect_timeout=10。
POSTGRES_URL_EXTRA_PARAMS=""
POSTGRES_CONTAINER_NAME="user-service-postgres-dev"

# 如果直接在宿主机执行 `npm run dev`，可以取消下面这一行注释。
# 使用 docker-compose.dev.yml 运行应用容器时，请保持注释。
# DATABASE_URL="postgresql://user_service:change-me@localhost:5432/user_service?schema=public"

# JWT
JWT_SECRET="dev-jwt-secret"
JWT_REFRESH_SECRET="dev-jwt-refresh-secret"

# Demo 第三方应用接入
USER_SERVICE_BASE_URL="http://localhost:3000"
DEMO_APP_BASE_URL="http://localhost:3000"
DEMO_APP_ID="demo-business-app"
DEMO_APP_SECRET="sk_demo_business_app_secret"

# Docker Compose 开发部署配置
USER_SERVICE_PORT="3000"
USER_SERVICE_CONTAINER_NAME="user-service-dev"
