# 数据库配置
# docker-entrypoint.sh 会根据下面拆分的 PostgreSQL 配置自动生成 DATABASE_URL。
# 如果需要完全自定义连接串，也可以直接配置 DATABASE_URL。
POSTGRES_HOST="db.example.com"
POSTGRES_PORT="5432"
POSTGRES_USER="user_service"
POSTGRES_PASSWORD="change-me"
POSTGRES_DB="user_service"
POSTGRES_SCHEMA="public"
# 可选。PostgreSQL 会话时区，docker-compose.yml 默认值为 Asia/Shanghai。
POSTGRES_TIMEZONE="Asia/Shanghai"
# 可选。外部 PostgreSQL 要求 SSL 连接时设置为 require。
POSTGRES_SSLMODE=""
# 可选。额外的 PostgreSQL URL 查询参数，例如 connect_timeout=10。
POSTGRES_URL_EXTRA_PARAMS=""

# JWT
JWT_SECRET="your-jwt-secret-key-change-in-production"
JWT_REFRESH_SECRET="your-jwt-refresh-secret-key-change-in-production"

# Demo 第三方应用接入
# 本地可执行 `npm run db:seed` 创建这个示例应用。
USER_SERVICE_BASE_URL="http://localhost:3000"
DEMO_APP_BASE_URL="http://localhost:3000"
DEMO_APP_ID="demo-business-app"
DEMO_APP_SECRET="sk_demo_business_app_secret"

# Docker Compose 部署配置
USER_SERVICE_IMAGE="ghcr.io/your-github-owner/user-service:latest"
USER_SERVICE_PORT="3000"
USER_SERVICE_CONTAINER_NAME="user-service"
